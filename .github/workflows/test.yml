name: PR Test

on:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: pnpm/action-setup@ea17c68df8912ef543352723c149a84f56e3d413 # v6.1.0
        with:
          run_install: false

      - name: Install Node.js
        run: pnpm env use --global 24
        shell: bash

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm run build

      - name: Verify single-file bundle
        run: |
          stray=$(find dist -type f ! -name index.html)
          if [ -n "$stray" ]; then
            echo "dist/ contains un-inlined assets:"
            echo "$stray"
            exit 1
          fi
          if grep -oE '(src|href)="[^"]*"' dist/index.html | grep -vE '="(https?:|data:|#)'; then
            echo "dist/index.html references external assets"
            exit 1
          fi

      - name: Lint
        run: pnpm run lint

      - name: Format check
        run: pnpm run fmt -- --check .

      - name: Run tests
        run: pnpm test
